#include<iostream>
using namespace std;
int main()
{
    int bin[10];
    int n;
    cout<<"Enter the number to convert into Decimal: ";
    cin>>n;
    // digit=sizeof(bin);
    
    cout<<dec;

    return 0;
}